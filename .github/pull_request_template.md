## What changed and why

<!-- Not just what — why this approach. -->

## Related issue

<!-- Fixes #... , or "None" -->

## Checklist

- [ ] `npm run lint` passes (frontend changes)
- [ ] `npm run build` passes (frontend changes)
- [ ] `cargo check` / `cargo clippy` passes (backend changes)
- [ ] `cargo test` passes (backend changes)
- [ ] Tested manually in `cargo tauri dev` (UI/UX changes)

## Anything you couldn't test

<!-- e.g. a provider you don't have API access to, or a distro/compositor you don't run -->
